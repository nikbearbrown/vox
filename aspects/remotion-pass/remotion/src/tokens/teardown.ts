// teardown — the NikBearBrown default palette, by its registry name. The values
// live in tokens/vox.ts (VOX *is* teardown after the 2026-07 default flip); this
// re-export exists so the palette can be imported as `teardown` alongside
// newsprint / neu / medhavy / humanitarians. See DESIGN.md.
export { VOX as TEARDOWN, DATAVIZ_CATEGORICAL, NEU_RED, FONT, SPRING_SMOOTH } from './vox';
